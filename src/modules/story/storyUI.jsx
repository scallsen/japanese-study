// Page-level constants the Story screens and layouts share. The module's
// Button lived here too until the design-system port replaced it with the
// shared Button; ACCENT is now ambient via ModuleThemeProvider.
export const BG = '#1E1E1E'
export const SURFACE = '#2A2A2A'
