import { TEXT_MUTED, FS_CAPTION } from '../../data/theme.js'

// Label above a form control. The field/select styles that used to live
// alongside it are gone — controls are the shared TextInput/Select now.
export const labelStyle = { fontSize: FS_CAPTION, color: TEXT_MUTED, letterSpacing: '0.08em', display: 'block', marginBottom: 4 }
